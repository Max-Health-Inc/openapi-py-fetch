name: CI

# Pushes to main are tested by publish.yml, which gates the release on the same
# job. This covers pull requests.

on:
  pull_request:
    branches: [main]

jobs:
  test:
    uses: ./.github/workflows/_test.yml
